specVersion: "v1"
serviceId: "my-electron-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "my-electron-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
